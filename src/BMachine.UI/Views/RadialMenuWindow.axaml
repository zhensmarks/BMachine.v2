<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="using:BMachine.UI.ViewModels"
        xmlns:models="using:BMachine.UI.Models"
        xmlns:controls="using:BMachine.UI.Controls"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="400"
        x:Class="BMachine.UI.Views.RadialMenuWindow"
        x:DataType="vm:RadialMenuViewModel"
        x:Name="RadialRoot"
        Width="200" Height="200"
        WindowStartupLocation="Manual"
        SystemDecorations="None"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        TransparencyLevelHint="Transparent"
        CanResize="False"
        Title="RadialMenu">

    <Window.Styles>
        <!-- Item Button Style -->
        <Style Selector="Button.MenuButton">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="ClipToBounds" Value="False"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Transitions">
                <Transitions>
                    <TransformOperationsTransition Property="RenderTransform" Duration="0:0:0.15"/>
                </Transitions>
            </Setter>
            <Setter Property="Template">
                <ControlTemplate>
                    <Grid>
                        <Border Name="PART_Bg"
                                Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1">
                            <Border.Transitions>
                                <Transitions>
                                    <BrushTransition Property="Background" Duration="0:0:0.15"/>
                                </Transitions>
                            </Border.Transitions>
                        </Border>
                        <ContentPresenter Name="PART_Content"
                                          Content="{TemplateBinding Content}"
                                          HorizontalContentAlignment="Center"
                                          VerticalContentAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter>
        </Style>
        
        <Style Selector="Button.MenuButton:pointerover">
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="RenderTransform" Value="scale(1.15)"/>
        </Style>

        <!-- Restore accent highlight effect: handled by the PieWedge slice instead -->
        <Style Selector="Button.MenuButton.highlighted /template/ Border#PART_Bg">
             <!-- Empty, let the PieWedge handle the glow beneath it -->
        </Style>

        <!-- Navigation Button (More/Back) -->
        <Style Selector="Button.NavButton">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="ClipToBounds" Value="False"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="Background" Value="{DynamicResource AppBackgroundBrush}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style Selector="Button.NavButton:pointerover">
             <Setter Property="Background" Value="#44FFFFFF"/>
             <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Styles>

    <Border Background="Transparent">
        <Grid>
             <!-- 1. Main Background Plate (Glassmorphism Core) -->
             <Border Width="140" Height="140" CornerRadius="70"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Background="{DynamicResource AppBackgroundBrush}" Opacity="0.5"
                     BorderBrush="#40FFFFFF" BorderThickness="1"/>

             <!-- 2. Spotlight Effect representing highlight over wedges -->
             <Canvas Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center"
                     IsHitTestVisible="False" ClipToBounds="True">
                 <!-- Clip path to keep spotlight strictly inside the main circle (140x140 at center of 200x200 canvas) -->
                 <Canvas.Clip>
                     <EllipseGeometry Rect="30,30,140,140" />
                 </Canvas.Clip>
                 <Ellipse Canvas.Left="{Binding SpotlightX}" Canvas.Top="{Binding SpotlightY}"
                          Width="100" Height="100" IsVisible="{Binding IsSpotlightVisible}">
                      <Ellipse.Fill>
                          <RadialGradientBrush Center="50%,50%" GradientOrigin="50%,50%" RadiusX="50%" RadiusY="50%">
                              <GradientStop Color="#40FFFFFF" Offset="0"/>
                              <GradientStop Color="#15FFFFFF" Offset="0.5"/>
                              <GradientStop Color="Transparent" Offset="1"/>
                          </RadialGradientBrush>
                      </Ellipse.Fill>
                 </Ellipse>
             </Canvas>

             <!-- 3. Segmented Divider Lines -->
             <Canvas Width="140" Height="140" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                 <!-- Line 1: North to South -->
                 <Line StartPoint="70,0" EndPoint="70,140" Stroke="#10FFFFFF" StrokeThickness="1"/>
                 <!-- Line 2: East to West -->
                 <Line StartPoint="0,70" EndPoint="140,70" Stroke="#10FFFFFF" StrokeThickness="1"/>
                 <!-- Line 3: Diagonal NW-SE -->
                 <Line StartPoint="20,20" EndPoint="120,120" Stroke="#10FFFFFF" StrokeThickness="1"/>
                 <!-- Line 4: Diagonal NE-SW -->
                 <Line StartPoint="120,20" EndPoint="20,120" Stroke="#10FFFFFF" StrokeThickness="1"/>
             </Canvas>

             <!-- 4. Planet Orbit Ring (Inner Divider) -->
             <Border Width="70" Height="70" CornerRadius="35"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     BorderBrush="#15FFFFFF" BorderThickness="1.5"
                     Background="#05000000"/>

             <!-- Hover Pie Wedge (Pizza Slice Highlight) -->
             <controls:PieWedge Width="140" Height="140"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                StartAngle="{Binding WedgeStart}" SweepAngle="{Binding WedgeSweep}"
                                InnerRadius="15" OuterRadius="70"
                                Fill="{DynamicResource AccentBlueBrush}" Opacity="0.5"
                                IsVisible="{Binding IsWedgeVisible}"
                                IsHitTestVisible="False"/>

             <!-- 5. Blank Central Hub -->
             <Border Width="30" Height="30" CornerRadius="15" 
                     BorderBrush="#25FFFFFF" BorderThickness="1.5"
                     Background="{DynamicResource CardBackgroundBrush}" Opacity="0.8"
                     HorizontalAlignment="Center" VerticalAlignment="Center"/>

             <!-- 6. Items on Canvas -->
             <ItemsControl ItemsSource="{Binding VisibleItems}" ClipToBounds="False">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Width="200" Height="200" ClipToBounds="False"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.Styles>
                    <Style Selector="ItemsControl > ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.Styles>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="models:RadialMenuItem">
                        <Grid Width="32" Height="32" ClipToBounds="False">
                            <!-- Regular script button -->
                            <Button Classes="MenuButton" 
                                    Classes.highlighted="{Binding IsHighlighted}"
                                    IsVisible="{Binding !IsNavigation}"
                                    Command="{Binding DataContext.ExecuteScript, ElementName=RadialRoot}" 
                                    CommandParameter="{Binding ScriptPath}">
                                <Grid>
                                    <!-- Icon with Drop Shadow for legibility -->
                                    <Viewbox Width="14" Height="14" IsVisible="{Binding Icon, Converter={x:Static ObjectConverters.IsNotNull}}">
                                         <Path Data="{Binding Icon}" Fill="{DynamicResource TextPrimaryBrush}" Stretch="Uniform">
                                             <Path.Effect>
                                                 <DropShadowEffect BlurRadius="4" Opacity="0.5" OffsetX="0" OffsetY="2" Color="Black"/>
                                             </Path.Effect>
                                         </Path>
                                    </Viewbox>
                                    
                                    <!-- Fallback Text -->
                                    <TextBlock Text="{Binding ShortName}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="10" FontWeight="Bold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               IsVisible="{Binding Icon, Converter={x:Static ObjectConverters.IsNull}}">
                                         <TextBlock.Effect>
                                             <DropShadowEffect BlurRadius="4" Opacity="0.5" OffsetX="0" OffsetY="2" Color="Black"/>
                                         </TextBlock.Effect>
                                    </TextBlock>
                                </Grid>
                            </Button>

                            <!-- Navigation button (More / Back) -->
                            <Button Classes="NavButton"
                                    IsVisible="{Binding IsNavigation}"
                                    IsHitTestVisible="False">
                                <TextBlock Text="{Binding ShortName}" 
                                           VerticalAlignment="Center" HorizontalAlignment="Center"
                                           FontSize="12" FontWeight="Bold"
                                           Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
             </ItemsControl>

             <!-- Hover Label (positioned further out to avoid overlapping icons) -->
             <Canvas Width="200" Height="200" ClipToBounds="False" IsHitTestVisible="False"
                     IsVisible="{Binding IsWedgeVisible}">
                 <Border Canvas.Left="{Binding HighlightLabelX}" 
                         Canvas.Top="{Binding HighlightLabelY}"
                         Background="{DynamicResource CardBackgroundBrush}"
                         BorderBrush="#30FFFFFF" BorderThickness="1"
                         CornerRadius="10" Padding="8,3"
                         BoxShadow="0 2 8 0 #40000000"
                         Margin="-40,-12,0,0"
                         MaxWidth="120">
                     <TextBlock Text="{Binding HighlightLabel}" 
                                Foreground="{DynamicResource TextPrimaryBrush}"
                                FontSize="10" FontWeight="SemiBold"
                                TextAlignment="Center"
                                TextWrapping="Wrap"
                                MaxLines="2"/>
                 </Border>
             </Canvas>
        </Grid>
    </Border>
</Window>
